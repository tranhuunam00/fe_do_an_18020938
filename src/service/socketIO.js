// import socketClient from "socket.io-client";
// import * as contants from "../constants/constants";

// var socket = socketClient(contants.SOCKET_IO);

// const socketIO = () => {
//   return socket;
// };

// export default socketIO;

// export const emitTest = () => {
//   return socket.emit("test", "tayto");
// };
